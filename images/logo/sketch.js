function setup() {
  createCanvas(displayWidth, displayHeight);
  background(255);
  textAlign(CENTER);
  
  
  stroke();
  fill(0, 0);
  textFont('Garamond', 35)
  
  text('Chic Corporations', width/2, height/2);
}